# 💧 ระบบติดตามข้อมูลน้ำควบแน่น (Condensate Monitoring System)

## 📋 ภาพรวม
ระบบนี้ใช้สำหรับเก็บข้อมูลและแสดงผลการติดตามน้ำควบแน่นจากโบยเลอร์ในรูปแบบ Dashboard แบบสมัยใหม่

## 📁 โครงสร้างไฟล์

```
test/
├── condensate_data.xlsx          # ไฟล์ Excel เก็บข้อมูลน้ำควบแน่น (Sheet: %condensate)
├── dashboard.py                  # โค๊ต Streamlit สำหรับแสดง Dashboard
├── create_condensate_data.py     # สคริปต์สร้างข้อมูลตัวอย่าง
├── requirements.txt              # ไลบรารี่ที่ต้องการ
└── README.md                     # ไฟล์นี้
```

## 🚀 วิธีการติดตั้ง

### 1. ติดตั้งไลบรารี่ที่ต้องการ
```bash
pip install -r requirements.txt
```

### 2. สร้างข้อมูลตัวอย่าง (ถ้าจำเป็น)
```bash
python create_condensate_data.py
```

## 📊 วิธีการใช้งาน

### เรียกใช้ Dashboard
```bash
streamlit run dashboard.py
```

หลังจากรันคำสั่งข้างต้น ระบบจะเปิด Browser โดยอัตโนมัติไปที่ `http://localhost:8501`

## ✨ ฟีเจอร์หลัก

### 📈 กราฟแสดงข้อมูล
- **ปริมาณน้ำควบแน่น**: แสดงแนวโน้มปริมาณน้ำตามวันที่
- **อุณหภูมิและความดัน**: แสดงการเปลี่ยนแปลงของสองตัวแปรพร้อมกัน
- **คุณภาพน้ำ (TDS)**: แสดงระดับสารละลายในน้ำ
- **สถานะการทำงาน**: แสดงเปอร์เซ็นต์ของการทำงาน ปกติ vs ผิดปกติ

### 📊 ข้อมูลสำคัญ (Key Metrics)
- ปริมาณน้ำรวมและค่าเฉลี่ยต่อวัน
- อุณหภูมิเฉลี่ย
- ความดันเฉลี่ย
- TDS เฉลี่ย
- จำนวนเหตุการณ์ผิดปกติ

### 🔍 ตัวกรองข้อมูล
- **ช่วงวันที่**: เลือกช่วงวันที่ที่ต้องการแสดง
- **สถานะ**: กรองเฉพาะข้อมูล "ปกติ" หรือ "ผิดปกติ" หรือทั้งสองอย่าง

### 📥 ดาวน์โหลด
- ดาวน์โหลดข้อมูลในรูปแบบ CSV
- ดาวน์โหลดข้อมูลในรูปแบบ Excel (.xlsx)

## 📋 โครงสร้างข้อมูลในไฟล์ Excel

ชีท: `%condensate`

| คอลัมน์ | ชนิดข้อมูล | คำอธิบาย |
|--------|-----------|---------|
| วันที่ | Date | วันที่บันทึกข้อมูล |
| เวลา | Time | เวลาบันทึกข้อมูล |
| ปริมาณน้ำควบแน่น (ลิตร) | Numeric | ปริมาณน้ำควบแน่นที่เก็บได้ |
| อุณหภูมิ (°C) | Numeric | อุณหภูมิของน้ำควบแน่น |
| ความดัน (bar) | Numeric | ความดันของระบบ |
| คุณภาพน้ำ (TDS) | Numeric | ระดับสารละลายในน้ำ (ppm) |
| หมายเหตุ | Text | สถานะ: "ปกติ" หรือ "ผิดปกติ" |

## 🔧 การปรับแต่งข้อมูล

### เพิ่มข้อมูลใหม่
1. เปิดไฟล์ `condensate_data.xlsx`
2. ไปยังชีท `%condensate`
3. เพิ่มแถวข้อมูลใหม่
4. บันทึกไฟล์
5. รีเฟรช Dashboard

### แก้ไขข้อมูลเดิม
1. เปิดไฟล์ `condensate_data.xlsx`
2. แก้ไขข้อมูลที่ต้องการ
3. บันทึกไฟล์
4. รีเฟรช Dashboard (กด F5 หรือ Refresh)

## 📱 การใช้งาน Sidebar

- **เลือกช่วงวันที่**: คลิกฟิลด์วันที่เพื่อเลือกช่วงที่ต้องการ
- **เลือกสถานะ**: คลิก checkbox เพื่อเลือกสถานะ

## 🐛 การแก้ไขปัญหา

### ปัญหา: ไฟล์ `condensate_data.xlsx` ไม่พบ
**วิธีแก้**: รันคำสั่ง `python create_condensate_data.py` เพื่อสร้างไฟล์ตัวอย่าง

### ปัญหา: ข้อผิดพลาด "ModuleNotFoundError"
**วิธีแก้**: รันคำสั่ง `pip install -r requirements.txt` เพื่อติดตั้งไลบรารี่ที่ขาดหายไป

### ปัญหา: Dashboard ไม่โหลดข้อมูล
**วิธีแก้**: 
1. ตรวจสอบว่าชีทชื่อ `%condensate` อยู่ในไฟล์ Excel
2. ตรวจสอบว่าชื่อคอลัมน์ตรงกับที่กำหนด

## 📞 การติดต่อ
สำหรับคำถามหรือปัญหา โปรดติดต่อฝ่าย IT Support

---
**สร้างเมื่อ**: 21 มกราคม 2026  
**เวอร์ชัน**: 1.0.0
