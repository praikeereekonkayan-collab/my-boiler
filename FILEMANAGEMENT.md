# 📂 โครงสร้างไฟล์และการจัดการข้อมูล (File Management)

## 📁 โครงสร้างปัจจุบัน

```
Desktop/test/
│
├── 🚀 แอปพลิเคชัน (Applications)
│   ├── dashboard.py           - Streamlit Dashboard (Main UI)
│   ├── run_dashboard.py       - Python Launcher
│   ├── run_dashboard.bat      - Windows Launcher
│   └── app.py                 - Legacy Checklist App
│
├── 📊 ข้อมูล (Data)
│   ├── condensate_data.xlsx         - ข้อมูลหลัก ⭐
│   ├── checklist_data.xlsx          - Checklist เก่า
│   ├── สมุดงาน1.xlsx                 - ข้อมูลเพิ่มเติม
│   └── condensate_report_*.txt      - รายงานประจำวัน
│
├── 🔧 สคริปต์ยูทิลิตี้ (Utilities)
│   ├── create_condensate_data.py    - สร้างข้อมูลตัวอย่าง
│   ├── add_condensate_data.py       - เพิ่มข้อมูลใหม่
│   └── generate_report.py           - สร้างรายงาน
│
├── 📖 เอกสารประกอบ (Documentation)
│   ├── README.md              - คู่มือทั่วไป
│   ├── QUICKSTART.md          - เริ่มต้นอย่างรวดเร็ว
│   ├── INSTALL.md             - คำแนะนำติดตั้ง
│   ├── INDEX.md               - ดัชนีไฟล์
│   ├── SUMMARY.md             - สรุประบบ
│   └── FILEMANAGEMENT.md      - ไฟล์นี้
│
└── 📦 การตั้งค่า (Configuration)
    └── requirements.txt       - ไลบรารี่ที่ต้องการ
```

---

## 📊 ไฟล์ Excel ที่สำคัญ

### condensate_data.xlsx ⭐ (สำคัญที่สุด)

**ชีท**: `%condensate`

| คอลัมน์ | ประเภท | ตัวอย่าง | หมายเหตุ |
|--------|-------|--------|---------|
| วันที่ | Date | 2026-01-21 | YYYY-MM-DD |
| เวลา | Time | 14:30 | HH:MM |
| ปริมาณน้ำควบแน่น (ลิตร) | Number | 247.12 | ตัวเลขทศนิยม |
| อุณหภูมิ (°C) | Number | 81.8 | 60-100 |
| ความดัน (bar) | Number | 5.69 | 3-8 |
| คุณภาพน้ำ (TDS) | Number | 1211 | 500-2000 |
| หมายเหตุ | Text | ปกติ | "ปกติ" หรือ "ผิดปกติ" |

**วิธีการเพิ่มข้อมูล**:
1. เปิดไฟล์ด้วย Microsoft Excel
2. ไปชีท `%condensate`
3. เพิ่มแถวใหม่ที่ด้านล่าง
4. กรอกข้อมูลตามรูปแบบ
5. บันทึก (Ctrl+S)
6. รีเฟรช Dashboard (F5)

---

## 🔄 การบำรุงรักษาไฟล์

### งานประจำวัน (Daily)
```
1. ตรวจสอบ Dashboard ว่ามีข้อมูลใหม่
2. หากมี ให้บันทึก screenshot
3. โน้ตเหตุการณ์ผิดปกติ (ถ้ามี)
```

### งานประจำสัปดาห์ (Weekly)
```
1. รัน: python add_condensate_data.py
2. ตรวจสอบแนวโน้มข้อมูล
3. อัปเดตรายงานประจำสัปดาห์
```

### งานประจำเดือน (Monthly)
```
1. สร้างรายงาน: python generate_report.py
2. ส่งรายงานให้หัวหน้า
3. ประเมินประสิทธิภาพระบบ
4. วางแผนปรับปรุง (ถ้าจำเป็น)
```

---

## 📋 ไฟล์ Python และหน้าที่

### dashboard.py (หลัก)
```python
import streamlit
import pandas
import plotly

# ฟังก์ชันหลัก:
- load_condensate_data()     # อ่านข้อมูล
- st.set_page_config()       # ตั้งค่าหน้า
- filtered_df                # ฟิลเตอร์ข้อมูล
- เพลงกราฟ                  # วาดกราฟ
- ปุ่มดาวน์โหลด              # export ข้อมูล
```

### create_condensate_data.py
```python
# สร้างข้อมูลตัวอย่าง 31 วัน
# ใช้ครั้งแรกเท่านั้น
# Output: condensate_data.xlsx
```

### add_condensate_data.py
```python
# เพิ่มข้อมูล 7 วันใหม่
# ใช้ประจำสัปดาห์
# Input: condensate_data.xlsx
# Output: condensate_data.xlsx (อัปเดต)
```

### generate_report.py
```python
# สร้างรายงานสรุป
# คำนวณสถิติ
# Output: condensate_report_YYYYMMDD.txt
```

---

## 🔐 การสำรองข้อมูล (Backup)

### วิธีสำรองข้อมูล
```bash
# สร้างโฟลเดอร์สำรอง
mkdir backup

# คัดลอกไฟล์หลัก
copy condensate_data.xlsx backup\condensate_data_YYYYMMDD.xlsx

# เพื่อความปลอดภัย เก็บในหลายที่:
- Local machine
- USB drive
- Cloud storage (OneDrive/Google Drive)
```

### ตัวอย่างเก็บ Backup
```
backup/
├── condensate_data_20260101.xlsx  (วันที่ 1)
├── condensate_data_20260108.xlsx  (วันที่ 8)
├── condensate_data_20260115.xlsx  (วันที่ 15)
└── condensate_data_20260121.xlsx  (วันที่ 21) ← ปัจจุบัน
```

---

## 🗑️ การจัดเก็บไฟล์เก่า

### ไฟล์ที่สามารถลบได้
```
❌ condensate_report_*.txt (เก่า >1 เดือน)
❌ .venv/ (virtual environment - สร้างใหม่ได้)
```

### ไฟล์ที่ต้องเก็บ
```
✅ condensate_data.xlsx  (ข้อมูลหลัก)
✅ dashboard.py          (โปรแกรมหลัก)
✅ requirements.txt      (ค่าตั้งค่า)
✅ *.md (เอกสาร)
```

---

## 📦 การโอน/ย้ายไฟล์

### โอนไปยังคนอื่น
```bash
# 1. สำรองข้อมูล
copy condensate_data.xlsx condensate_data_BACKUP.xlsx

# 2. ไปที่โฟลเดอร์ test
# 3. เลือกไฟล์ที่ต้องการ
# 4. ส่งทาง Email/Cloud/USB

# ไฟล์ที่ต้องส่ง:
- condensate_data.xlsx
- dashboard.py
- run_dashboard.py
- run_dashboard.bat
- requirements.txt
- *.md (เอกสารทั้งหมด)
```

---

## 🔍 ตรวจสอบความสมบูรณ์

### Checklist ไฟล์
```
✅ dashboard.py         - มี?
✅ condensate_data.xlsx - มี?
✅ requirements.txt     - มี?
✅ README.md            - มี?
✅ run_dashboard.py     - มี?
```

### ตรวจสอบข้อมูล
```bash
# ดูจำนวนแถว
python -c "import pandas as pd; df = pd.read_excel('condensate_data.xlsx'); print(f'Rows: {len(df)}')"

# ดูคอลัมน์
python -c "import pandas as pd; df = pd.read_excel('condensate_data.xlsx'); print(df.columns.tolist())"
```

---

## 📊 สรุปข้อมูล

### ข้อมูลปัจจุบัน
- **ไฟล์หลัก**: condensate_data.xlsx
- **จำนวนบันทึก**: 31+
- **ช่วงวันที่**: 2025-12-22 ถึง 2026-01-21
- **ขนาดไฟล์**: ~6.62 KB

### ขนาด Dashboard
- **ไฟล์โปรแกรม**: 10.20 KB
- **เวลาโหลด**: <2 วินาที
- **ความเร็ว**: เร็วมาก

---

## ⚠️ ข้อมูลที่ต้องหลีกเลี่ยง

### ❌ อย่าลบ
```
- condensate_data.xlsx    (ข้อมูลกำลังใช้)
- dashboard.py            (โปรแกรมหลัก)
- requirements.txt        (ค่าตั้งค่า)
```

### ❌ อย่าแก้ไข
```
- dashboard.py            (ถ้าไม่มั่นใจ)
- requirements.txt        (อาจทำให้โปรแกรมเสีย)
```

### ⚠️ ระวัง
```
- ชื่อชีท Excel ต้องชื่อ %condensate เท่านั้น
- ชื่อคอลัมน์ต้องตรงกัน
- วันที่ต้องเป็น YYYY-MM-DD
```

---

## 🎯 Best Practices

### ✅ ทำ
```
1. สำรองข้อมูลประจำสัปดาห์
2. ตรวจสอบข้อมูลก่อนเก็บ
3. บันทึกรายงานประจำเดือน
4. เพิ่มหมายเหตุเมื่อมีความผิดปกติ
```

### ❌ อย่าทำ
```
1. ลบไฟล์โปรแกรมหลัก
2. แก้ไขชื่อไฟล์สำคัญ
3. ทำการเปลี่ยนโครงสร้าง Excel
4. ใช้ข้อมูลเดิมซ้ำแล้วซ้ำเล่า
```

---

## 📞 ติดต่อสำหรับ

- **การเพิ่มคอลัมน์**: IT Development
- **การสำรองข้อมูล**: IT Admin
- **ข้อมูล/รายงาน**: หัวหน้าแผนก
- **ปัญหาทั่วไป**: IT Support

---

**เอกสารเวอร์ชัน**: 1.0  
**อัปเดต**: 21 มกราคม 2026
