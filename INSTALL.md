# 📦 คำแนะนำการติดตั้ง (Installation Guide)

## ✅ ข้อกำหนด (Requirements)

- **ระบบปฏิบัติการ**: Windows 10/11, macOS, Linux
- **Python**: 3.8+
- **ดิสก์**: 50 MB ว่าง
- **หน่วยความจำ**: 512 MB ขึ้นไป

---

## 🚀 ขั้นตอนการติดตั้ง

### ขั้นตอนที่ 1: ยืนยันว่ามี Python
```bash
python --version
```
ถ้าแสดง `Python 3.x.x` แสดงว่าติดตั้งแล้ว

### ขั้นตอนที่ 2: ไปที่โฟลเดอร์ test
```bash
cd Desktop\test
```

### ขั้นตอนที่ 3: ติดตั้งไลบรารี่ (ครั้งแรกเท่านั้น)
```bash
pip install -r requirements.txt
```

### ขั้นตอนที่ 4: เรียกใช้ Dashboard
```bash
python run_dashboard.py
```

### ขั้นตอนที่ 5: เปิดเบราว์เซอร์
ระบบจะแสดง URL: `http://localhost:8501`

---

## 🔧 วิธีการดำเนินการ (Step-by-Step)

### สำหรับผู้ใช้ Windows (ง่ายที่สุด)

#### วิธีที่ 1: ดับเบิลคลิก
1. เปิดโฟลเดอร์ `Desktop\test`
2. หาไฟล์ `run_dashboard.bat`
3. ดับเบิลคลิก
4. Dashboard จะเปิดในเบราว์เซอร์

#### วิธีที่ 2: Command Prompt
1. เปิด Command Prompt
2. พิมพ์:
```bash
cd C:\Users\<username>\Desktop\test
python run_dashboard.py
```
3. กด Enter
4. รอสักครู่

#### วิธีที่ 3: PowerShell
1. เปิด PowerShell
2. พิมพ์:
```powershell
cd $env:USERPROFILE\Desktop\test
python run_dashboard.py
```

---

## 🌍 สำหรับผู้ใช้ macOS/Linux

```bash
# ไปที่โฟลเดอร์
cd ~/Desktop/test

# ติดตั้งไลบรารี่
pip3 install -r requirements.txt

# รัน Dashboard
python3 run_dashboard.py
```

---

## 📝 ไฟล์ที่จำเป็น

ตรวจสอบว่ามีไฟล์เหล่านี้ในโฟลเดอร์ `test`:

```
✅ condensate_data.xlsx    - ไฟล์ข้อมูล
✅ dashboard.py            - โปรแกรม Dashboard
✅ run_dashboard.py        - ตัวเรียกใช้
✅ run_dashboard.bat       - ไฟล์เบตช์ Windows
✅ requirements.txt        - รายชื่อไลบรารี่
```

---

## 🔗 เชื่อมต่อ

### URL ของ Dashboard
- **Local**: `http://localhost:8501`
- **Network**: `http://<IP>:8501` (ถ้าเปิด Network URL)

### การหา IP Address
```bash
# Windows
ipconfig

# macOS/Linux
ifconfig
```

---

## ⚙️ การตั้งค่าเพิ่มเติม

### หากต้องการเปิด Network Access (แชร์ให้คนอื่น)

แก้ไข `run_dashboard.py`:

เปลี่ยนจาก:
```python
subprocess.run([sys.executable, "-m", "streamlit", "run", "dashboard.py"])
```

เป็น:
```python
subprocess.run([sys.executable, "-m", "streamlit", "run", "dashboard.py", "--server.address", "0.0.0.0"])
```

---

## 🐛 การแก้ไขปัญหา

### ❌ ข้อผิดพลาด: "ModuleNotFoundError"

**สาเหตุ**: ไลบรารี่ยังไม่ติดตั้ง

**วิธีแก้**:
```bash
pip install -r requirements.txt
```

---

### ❌ ข้อผิดพลาด: "Port 8501 is already in use"

**สาเหตุ**: Dashboard ปิดไม่ถูกต้อง

**วิธีแก้**:
```bash
# Windows
netstat -ano | findstr :8501
taskkill /PID <PID> /F

# macOS/Linux
lsof -i :8501
kill -9 <PID>
```

---

### ❌ ข้อผิดพลาด: "Sheet %condensate not found"

**สาเหตุ**: ชีท Excel ชื่อไม่ตรง

**วิธีแก้**:
1. เปิด `condensate_data.xlsx`
2. ตรวจสอบชื่อชีท
3. ตรวจสอบในไฟล์ `dashboard.py` ว่าตรงกัน

---

### ❌ ข้อผิดพลาด: "Python is not recognized"

**สาเหตุ**: Python ไม่ติดตั้งหรือไม่ได้เพิ่ม PATH

**วิธีแก้**:
1. ติดตั้ง Python ใหม่จาก python.org
2. ขณะติดตั้ง ตรวจสอบ "Add Python to PATH"

---

## 🔄 การอัปเดต

### ติดตั้ง Streamlit เวอร์ชันใหม่
```bash
pip install --upgrade streamlit
```

### ติดตั้งไลบรารี่ทั้งหมดใหม่
```bash
pip install --upgrade -r requirements.txt
```

---

## ✅ ทดสอบการติดตั้ง

หลังจากติดตั้ง ให้รันคำสั่งเหล่านี้:

```bash
# ตรวจสอบ Python
python --version

# ตรวจสอบ Streamlit
streamlit --version

# ตรวจสอบ Pandas
python -c "import pandas; print(pandas.__version__)"

# ตรวจสอบ Plotly
python -c "import plotly; print(plotly.__version__)"
```

---

## 📦 ขนาดไฟล์หลังติดตั้ง

```
โค้ดต้นฉบับ:       ~47 KB
Virtual Env:      ~200 MB (ตัวเลือก)
ข้อมูล:           ~10 KB
รวมทั้งสิ้น:      ~250 MB
```

---

## 🎯 เสร็จสิ้น!

หากทำตามขั้นตอนแล้ว Dashboard ควรจะทำงาน ✅

ถ้ามีปัญหา:
1. ตรวจสอบ error message
2. ดูส่วน "Troubleshooting"
3. ติดต่อ IT Support

---

**คู่มือเวอร์ชัน**: 1.0  
**อัปเดตล่าสุด**: 21 มกราคม 2026
